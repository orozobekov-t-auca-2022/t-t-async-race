export type Car = {
  id: number;
  name: string;
  color: string;
};

export type CreateCarRequest = {
  name: string;
  color: string;
};

export type UpdateCarRequest = {
  name: string;
  color: string;
};
